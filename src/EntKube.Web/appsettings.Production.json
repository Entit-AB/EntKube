{
  "DatabaseProvider": "Postgres",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=entkube;Username=entkube;Password=CHANGE_ME",
    "// TelemetryConnection": "Set via ConnectionStrings__TelemetryConnection (docker-compose provides it, pointing at the postgres-telemetry service). Left EMPTY by default so the native telemetry store disables cleanly when unconfigured instead of hammering localhost:5432.",
    "TelemetryConnection": ""
  },
  "Telemetry": {
    "RetentionDays": 14,
    "PublicIngestUrl": "",
    "EnableTextSearchIndex": false,
    "UnloggedTables": false,
    "IngestMaxRequestsPerMinute": 0,
    "TenantRetentionDays": {}
  }
}
